@model List<Category>
<section id="portfolio" class="portfolio section light-background">

    <div class="container section-title" data-aos="fade-up">
        <h2>Çalışmalarım</h2>
        <p>Kendi çalışmalarım olan projelerimi ilgili örnek ve resimlerle aşağıdan erişim sağlayabilirsiniz. </p>
    </div>
    <div class="container">
        <div class="isotope-layout" data-default-filter="*" data-layout="masonry" data-sort="original-order">
            <ul class="portfolio-filters isotope-filters" data-aos="fade-up" data-aos-delay="100">
                <li data-filter="*" class="filter-active">Tümü</li>
                @foreach(var item in Model)
                {
                    <li data-filter=".filter-@item.CategoryId">@item.CategoryName</li>
                }
               
            </ul>
           <!-- End Portfolio Container -->
            @await Component.InvokeAsync("_DefaultMyProjectComponentPartial")
        </div>

    </div>

</section><!-- /Portfolio Section -->